[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "valis-reproduction-build"
version = "0.1.0"
description = "Evidence-driven source rebuild pipeline for the PC-88 Mugen Senshi Valis localization"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

