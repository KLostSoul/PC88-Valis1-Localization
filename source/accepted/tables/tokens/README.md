# 토큰 원천 규칙

이 디렉터리의 제어 토큰 표는 의미가 확정된 문맥만 기록한다. 문자처럼
보인다는 이유로 제어 바이트를 문자로 매핑하지 않으며, `10 11`/`10 12`를
전역 padding으로 취급하지 않는다. 이벤트 `raw-changes.csv`, 엔딩
`text-basecode-tokens.csv`, 게임오버 `token_pairs`는 각 사용처의 리터럴
토큰열 원천이다.
